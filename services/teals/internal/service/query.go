package service

type QueryService struct {
	// TODO: service for audit event queries, e.g. by event ID, by producer key ID, by time range, etc.
	// will use the ledger service to build inclusion proofs for the queried events
}
